// constrain

// action

// reducers
const INITIAL_VALUE = {
	loginStatus: 1,
};

const User = (state = INITIAL_VALUE, action) => {
	switch (action.type) {
		default: {
			return state;
		}
	}
};

export default User;
