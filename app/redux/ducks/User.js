// constrain

// action

// reducers
const INITIAL_VALUE = {
	loginStatus: 'LOGGED',
};

const User = (state = INITIAL_VALUE, action) => {
	switch (action.type) {
		default: {
			return state;
		}
	}
};

export default User;
